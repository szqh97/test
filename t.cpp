#include <iostream>
#include <stdio.h>
using namespace std;


int main ( int argc, char *argv[] )
{
    FILE *fp = fopen("./tttt", "w");
    fclose(fp);
    fclose(fp);
    return 0;
}			/* ----------  end of function main  ---------- */
